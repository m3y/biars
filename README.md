# biars

## Tasks
```
$ just usage
Available recipes:
    applying-sample-application # applying sample application
    setup                       # alias for `applying-sample-application`
    audit                       # audit
    boot-local-cluster          # boot local cluster
    boot                        # alias for `boot-local-cluster`
    build                       # build
    clean                       # clean
    clean-all                   # clean all
    fmt                         # format
    lint                        # lint
    run                         # run
    shutdown-local-cluster      # shutdown local cluster
    test                        # test
    usage                       # usage
    watch +COMMAND='test'       # watch
```
